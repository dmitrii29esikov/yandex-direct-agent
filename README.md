# Yandex Direct Agent

MCP-сервер на Python для управления Яндекс.Директом, Яндекс.Метрикой,
Яндекс Тег Менеджером и анализа сайтов — через ИИ-агентов (Cursor,
Claude Desktop) или командную строку.

## Возможности

- **Яндекс.Директ** — кампании, отчёты по статистике (показы, клики, CTR, CPC, расход), конверсии, CPA, цели Метрики, атрибуция.
- **Яндекс.Метрика** — счётчики с классификацией (metrica / yandex_business / ga), цели счётчиков.
- **Яндекс Тег Менеджер (YTM)** — теги, триггеры, переменные, аудит контейнера, снимки состояния и diff изменений.
- **Анализ сайтов** — формы, счётчики Метрики/GA/YTM, `dataLayer.push`, inline-обработчики, перехват сетевых запросов через Playwright (работает с SPA).
- **Обогащение данных** — подстановка имён вместо голых ID (кампании, счётчики, цели).
- **Аудит выгрузок Директ Коммандера** — чтение XLSX/XLS выгрузок, проверка по правилам Директа, отчёты в Excel и CSV.

## Инструменты (32)

> Было 18, стало 32: добавлены мультиаккаунтность, онлайн-аудит и правки
> с dry-run — см. дополнение от 21.09.2026 (вечер) в конце файла.

### Яндекс.Директ
| Инструмент | Что делает |
|---|---|
| `get_campaigns` | Список кампаний с параметрами |
| `check_api_connection` | Проверка подключения к API Директа |
| `get_campaign_stats` | Отчёты: показы, клики, CTR, CPC, конверсии, CPA |
| `audit_direct_commander_files` | Аудит XLSX/XLS выгрузок из Директ Коммандера |

### Яндекс.Метрика
| Инструмент | Что делает |
|---|---|
| `get_metrica_counters` | Счётчики с классификацией |
| `get_metrica_goals` | Цели конкретного счётчика |

### Обогащение (ID → имя)
| Инструмент | Что делает |
|---|---|
| `get_campaign_names` | ID кампаний → названия |
| `get_counter_names` | ID счётчиков → названия |
| `get_goal_names` | ID целей → названия |

### Яндекс Тег Менеджер
| Инструмент | Что делает |
|---|---|
| `get_ytm_tags` | Теги контейнера |
| `get_ytm_triggers` | Триггеры контейнера |
| `get_ytm_variables` | Переменные (можно только пользовательские) |
| `audit_ytm_container` | Аудит: теги без триггеров, неиспользуемые переменные, дубликаты |

### Анализ сайтов
| Инструмент | Что делает |
|---|---|
| `fetch_page` | Забрать HTML (с рендером JS) |
| `analyze_site` | Полный анализ: формы, счётчики, dataLayer, события |

### Аудит изменений
| Инструмент | Что делает |
|---|---|
| `snapshot_ytm` | Снять снимок контейнера YTM в JSON |
| `list_snapshots` | Список сохранённых снимков |
| `audit_ytm_changes` | Diff текущего состояния с последним снимком |

### Аудит выгрузок Директ Коммандера
| Инструмент | Что делает |
|---|---|
| `audit_direct_commander_files` | Аудит XLSX/XLS выгрузок: чтение всех листов, проверка по правилам Директа, отчёты в Excel и CSV |

**Что проверяет `audit_direct_commander_files`:**

- Пустые обязательные поля (`Доп. объявление группы`, `Название группы`).
- Некорректное `Доп. объявление группы` (должно быть `-` или `+`).
- `Номер группы` — только цифры.
- Ключевые фразы: длина до 4096, слово до 35 символов.
- Заголовки 1–7: длина до 56, слово до 22.
- Тексты 1–3: длина до 81, слово до 23, знаков препинания до 15.
- Отображаемая ссылка: длина до 20, без домена/протокола.
- Ссылка: обязательно `http://` или `https://`.
- Быстрые ссылки: до 8 штук, заголовок до 30, описание до 60, без `!`, `?`, `[`, `]`.
- Уточнения: до 25 штук, каждое до 25 символов.
- Метки: до 25 символов.
- Минус-фразы на группу: до 4096 символов, одна фраза — до 7 слов.
- Минус-фразы на кампанию: до 20000 символов.

**Что выводит `audit_direct_commander_files`:**

Один Excel-файл `audit_report.xlsx` с листами:

| Лист | Что внутри |
|---|---|
| `Оглавление` | Описание всех разделов отчёта |
| `Сводка` | Ключевые цифры: файлы, объявления, группы, проблемы |
| `1_Аудит_проблемы` | Все найденные проблемы с координатами |
| `2_Паспорт_кампаний` | Паспорт кампании |
| `3_Объявления` | Полная выгрузка всех объявлений |
| `4_Изображения` | Анализ изображений |
| `5_Фразы` | Ключевые фразы, аудитории, автотаргетинг |
| `6_Минус_фразы` | Минус-фразы кампании и групп |
| `Файлы_и_листы` | Прочитанные листы |
| `Метаданные` | Метаданные кампаний |
| `Справочник_Регионы` | Имена регионов |
| `Справочник_Поля` | Словарь значений полей |

Плюс 4 CSV:

- `audit_report_issues.csv` — проблемы
- `audit_report_ads.csv` — объявления
- `audit_report_phrases.csv` — фразы
- `audit_report_minus.csv` — минус-фразы

## Установка

```bash
git clone https://github.com/dmitrii29esikov/yandex-direct-agent.git
cd yandex-direct-agent
pip install -r requirements.txt
python -m playwright install chromium
================================================================
ДОПОЛНЕНИЕ ОТ 20.09.2026 — НАЙДЕННЫЕ ОШИБКИ И ИСПРАВЛЕНИЯ
================================================================

НАЙДЕННЫЕ ОШИБКИ

Кириллица в пути проекта.
Проект лежал в C:\Users\Дмитрий\Desktop\yandex_direct_agent.
Из-за русских букв Chatbox и Cursor передавали Python путь в виде
кракозябр (╨Ф╨╝╨╕╤В╤А╨╕╨╣) — файл server.py "не находился".

Дубликат папки проекта.
Появились две копии: на Рабочем столе и в C:.
Программы читали разные версии файлов — правки не срабатывали.

Главный баг: пустой список инструментов (tools: []).
В файлах tools/*.py был импорт from server import mcp.
Python при этом создавал ВТОРОЙ экземпляр FastMCP — инструменты
вешались на него, а mcp.run() запускал ПЕРВЫЙ (пустой) экземпляр.
Результат: сервер отвечает, но "17 tools" не появляется.
Именно поэтому в Cursor сервер работал, а инструменты не активировались.

start_mcp.bat содержал старый путь + BOM + кириллицу.
Chatbox запускал не тот server.py.

ИСПРАВЛЕНИЯ

Рабочая папка перенесена в C:\yandex_direct_agent (без кириллицы).
Старая копия на Рабочем столе удалена.

Создан общий модуль mcp_instance.py — единый объект mcp и api_client:

from mcp.server.fastmcp import FastMCP
from api_client import YandexDirectAPIClient

mcp = FastMCP("YandexDirectPro")
api_client = YandexDirectAPIClient()

server.py переписан — берёт mcp из mcp_instance:

from mcp_instance import mcp
from tools import campaigns, metrica, reports, enrich, tag_manager, site_parser, audit, audit_files

if name == "main":
mcp.run()

Во ВСЕХ файлах tools/*.py первая строка заменена:
было: from server import mcp, api_client
стало: from mcp_instance import mcp, api_client

start_mcp.bat пересоздан (ASCII, без BOM, правильный путь):

@echo off
"C:\Program Files\Python314\python.exe" "C:\yandex_direct_agent\server.py"

ИТОГОВЫЕ НАСТРОЙКИ MCP (Chatbox / Cursor)

Name: yandex-direct-agent
Type: stdio (Локально)
Command: C:\yandex_direct_agent\start_mcp.bat
Переменные окружения: пусто
Ожидаемый результат: 18 tools

ДИАГНОСТИКА

Проверить количество инструментов:

"C:\Program Files\Python314\python.exe" -c "from mcp_instance import mcp; from tools import campaigns, metrica, reports, enrich, tag_manager, site_parser, audit, audit_files; print('tools count:', len(mcp._tool_manager._tools))"

Ожидаем: tools count: 18

Проверить импорты в файлах:

findstr /n "mcp_instance" C:\yandex_direct_agent\server.py C:\yandex_direct_agent\tools*.py

В каждом файле должна быть строка с mcp_instance.

ПРАВИЛА НА БУДУЩЕЕ

Рабочая папка только одна: C:\yandex_direct_agent. Копии не создавать.

mcp_instance.py — ядро фикса, не удалять.

Любые новые tools/*.py импортируют mcp и api_client ТОЛЬКО из mcp_instance.

Токены и ключи — только в .env, в этом README не писать.

Новый чат начинать фразой: "Читай PROJECT.md и README.md в C:\yandex_direct_agent"


================================================================
ДОПОЛНЕНИЕ ОТ 21.09.2026 — НОВЫЙ ИНСТРУМЕНТ И ИСПРАВЛЕНИЯ
================================================================

НОВЫЙ ИНСТРУМЕНТ
----------------
audit_direct_commander_files — аудит выгрузок Директ Коммандера (XLSX/XLS).

Файл:        tools/audit_files.py
Импорт:      from mcp_instance import mcp
Декоратор:   @mcp.tool()
Функция:     audit_direct_commander_files(path, json_output=False)
Зависимости: pandas, openpyxl, xlrd (добавлены в requirements.txt)
Подключён:   server.py → from tools import ..., audit_files

Результат: инструментов стало 18 (было 17).

ЧТО ДЕЛАЕТ
----------
- Читает ВСЕ листы XLSX/XLS: Тексты, Регионы, Словарь значений полей.
- Автоматически находит шапку (в т.ч. двухуровневую).
- Парсит метаданные кампании: тип, № заказа, валюта, объект продвижения.
- Расшифровывает регионы через лист «Регионы».
- Проверяет по правилам Директа: длины, слова, знаки препинания, URL,
  быстрые ссылки, уточнения, метки, минус-фразы.
- Разбирает минус-фразы кампании (из метаданных) и групп (из колонки).
- Разбирает ключевые фразы, аудитории (audience:...) и автотаргетинг (---).
- Сохраняет отчёт в audit_report.xlsx (12 листов) + 4 CSV.

ЛИСТЫ ОТЧЁТА
------------
Оглавление, Сводка, 1_Аудит_проблемы, 2_Паспорт_кампаний,
3_Объявления, 4_Изображения, 5_Фразы, 6_Минус_фразы,
Файлы_и_листы, Метаданные, Справочник_Регионы, Справочник_Поля.

CSV ОТЧЁТЫ
----------
audit_report_issues.csv  — проблемы
audit_report_ads.csv     — объявления
audit_report_phrases.csv — фразы
audit_report_minus.csv   — минус-фразы

ИСПРАВЛЕНИЯ ОТ 21.09.2026
-------------------------
1. Исправлен блок «Длина» в _build_columns:
   - Колонки «заголовок 1», «заголовок 2», «текст» больше не попадают в отчёт.
   - Добавлен флаг inside_length_block.

2. Убрана ложная проверка «Дубли минус-фраз» из _check_row:
   - Она срабатывала на каждой строке группы (2104 ложных предупреждения).

3. Исправлена кодировка вывода JSON:
   - В main() добавлено:
         import sys
         sys.stdout.reconfigure(encoding="utf-8")
   - Иначе Python падал с UnicodeEncodeError на cp1251.

4. Исправлены лимиты быстрых ссылок:
   - QUICK_MAX_ITEMS: 4 → 8 (по справке Директа).
   - Убрана проверка QUICK_TITLE_TOTAL (суммарного лимита в справке нет).
   - Добавлена проверка запрещённых символов `!`, `?`, `[`, `]`.

5. Убрана ложная проверка duplicate_ad_id:
   - Повтор ID объявления на разных фразах — норма для Директ Коммандера.

6. Добавлены листы 5_Фразы и 6_Минус_фразы в audit_report.xlsx.
7. Добавлены CSV: audit_report_phrases.csv, audit_report_minus.csv.

УДАЛЕНИЕ СТАРОЙ ПАПКИ
---------------------
Папка C:\yandex_agent (без audit_files.py и без start_mcp.bat)
удалена командой: rmdir /S /Q C:\yandex_agent

РЕЗУЛЬТАТ АУДИТА (образец data/710718813.xlsx)
---------------------------------------------
Файлов: 1 | объявлений: 730 | групп: 16 | проблем: 1458
(ошибок: 906, предупреждений: 552)
Уникальных изображений: 95 | регионов в справочнике: 15137
Всего фраз: 666 | всего минус-фраз: 2349

ИТОГ
----
Инструментов: 18.
Коммиты: fa81e8e, cee5d93 — запушены в GitHub.


================================================================
ДОПОЛНЕНИЕ ОТ 21.09.2026 (ВЕЧЕР) — МУЛЬТИАККАУНТНОСТЬ И ОНЛАЙН-АУДИТ
================================================================

ЗАЧЕМ
-----
Работа с несколькими клиентскими аккаунтами одновременно: Директ, Метрика,
Тег Менеджер. Владелец выдаёт доступ нам как представителю — мы работаем с
любым аккаунтом, выбирая его НА УРОВНЕ ВЫЗОВА инструмента, без перезапуска
сервера и без правок кода.

ЧТО ПРОВЕРЕНО В СПРАВКЕ ЯНДЕКСА (факты, не догадки)
--------------------------------------------------
Директ:  Client-Login работает ТОЛЬКО у агентства. Вне агентства один токен
         на аккаунт. Use-Operator-Units: true — тратим свои баллы, а не
         баллы клиента.
Метрика: GET-параметр ulogin — работа с аккаунтами представительского
         доступа. Один токен + ulogin = любой аккаунт.
YTM:     доступ определяется токеном (scope ytm:read), переключения логина
         нет. API ТОЛЬКО ДЛЯ ЧТЕНИЯ — все методы GET, записи нет вообще.
         Поэтому «исправить в Tag Manager» через API невозможно в принципе:
         такие задачи выдаются чек-листом для интерфейса (пометка ytm_ui).
Бонус:   GET /management/v1/clients?counters=... отдаёт поле chief_login —
         готовый логин для Client-Login. Автоматическая связка
         «счётчик Метрики <=> аккаунт Директа».

АРХИТЕКТУРА
-----------
data/accounts.json     — реестр аккаунтов: доступы и привязки, БЕЗ токенов
secrets/secrets.json   — токены (в .gitignore, в git НЕ попадает)
access.py              — ЕДИНСТВЕННОЕ место с логикой agency/token
accounts_store.py      — реестр, секрет-стор, разрешение ID -> аккаунт
audit_engine/          — движок аудита (base, fetchers, checks_*, diagnose, report)
tools/accounts.py      — инструменты онбординга аккаунтов
tools/audit_online.py  — онлайн-аудит по API (без выгрузок Коммандера)

ГИБРИДНЫЕ РЕЖИМЫ ДОСТУПА (подход 3)
-----------------------------------
mode=agency — один агентский токен + заголовок Client-Login +
              Use-Operator-Units (баллы наши). Переключение мгновенное.
mode=token  — свой токен аккаунта (владелец выдал доступ представителю).
Оба режима обслуживает ОДИН код: разница только в записи реестра.
Перевод клиента с token на agency — правка одного поля, без переделок.

НОВЫЕ ИНСТРУМЕНТЫ (18 -> 32)
----------------------------
Управление аккаунтами (8):
  list_accounts          — реестр: режим, счётчики, контейнеры, токены маской
  check_account          — health-check трёх контуров с понятным диагнозом
  describe_account       — паспорт аккаунта
  discover_accounts      — автопоиск всего, что нам уже выдали
  add_account            — подключить новый аккаунт
  set_account_tokens     — обновить токены (клиент перевыпустил доступ)
  link_account_data      — привязать счётчики Метрики и контейнеры YTM
  oauth_link             — ссылка «Разрешить» для владельца аккаунта
Аудит (6):
  audit_account          — полный аудит одного аккаунта
  audit_all_accounts     — аудит всех аккаунтов реестра по очереди
  list_audit_checks      — список всех проверок
  apply_audit_fixes      — применение автоматических правок (dry-run + confirm)
  rollback_apply         — откат применённых правок по журналу
  list_audit_fixers      — какие правки агент умеет применять сам

ИЗМЕНЁННЫЕ ИНСТРУМЕНТЫ
----------------------
Во ВСЕ существующие инструменты добавлен параметр account. Принимает имя
аккаунта ИЛИ любой знакомый ID: ID счётчика Метрики, ID контейнера YTM,
client_login, client_id. Не указан — берётся DEFAULT_ACCOUNT.
Изменены: get_campaigns, check_api_connection, get_campaign_stats,
get_campaign_names, get_counter_names, get_goal_names, get_metrica_counters,
get_metrica_goals, get_ytm_tags, get_ytm_triggers, get_ytm_variables,
audit_ytm_container, snapshot_ytm, audit_ytm_changes.
tools/audit_files.py (файловый аудит Коммандера) — НЕ ТРОГАЛИ, работает как было.

ПРИОРИТИЗАЦИЯ: impact 0..100
---------------------------
impact = база(severity: error 40 / warning 20 / info 5)
       + деньги под риском (логарифм расхода, до 25)
       + расход есть, конверсий нет (+25)
       + блокирует другие исправления (+15)
       + auto +5 / manual 0 / ytm_ui -5

ДИАГНОЗЫ
--------
Отдельный слой diagnose(): склеивает находки в корневые причины с блоками
«Почему это важно», «Последствия» и упорядоченными действиями. Человек
читает сверху вниз и делает по порядку.

ЧТО ВЫВОДИТ audit_account
-------------------------
- markdown в чат: топ-N диагнозов, сводка по кодам, раздел «Не проверено»;
- data/audits/audit_<account>_<дата>.xlsx:
  листы «Диагнозы», «Находки», «Не проверено», «Связка»;
- data/audits/remediation_<account>_<дата>.md — чек-лист правок.

РЕЖИМЫ
------
report — только чтение, по умолчанию.
apply  — реализован, но ТОЛЬКО с двойным предохранителем:
         без confirm=True инструмент возвращает план (dry-run) и НЕ отправляет
         ни одного запроса на изменение. Применяются только находки с
         fixable=auto. Каждая правка до применения описана парой before/after,
         после применения пишется журнал data/audits/apply_*.json,
         откат — инструментом rollback_apply.

ПРОВЕРКИ (29)
-------------
direct (14): структура, тексты, ставки/бюджет, модерация
  NO_ACTIVE_CAMPAIGNS, EMPTY_CAMPAIGN, ADGROUP_NO_ADS, ADGROUP_NO_KEYWORDS,
  DUPLICATE_KEYWORDS, GROUP_NO_NEGATIVES, TEXT_LENGTH, NO_SITELINKS,
  NO_CALLOUTS, NO_DAILY_BUDGET, SPEND_NO_CONVERSION, CPA_ABOVE_TARGET,
  REJECTED_ADS, STATUS_PAYMENT
metrica (6): NO_COUNTER, NO_GOALS, NO_FAVORITE_GOALS, NO_DATA, LOW_ACTIVITY,
  CODE_STATUS
ytm (5): EMPTY_CONTAINER, TAGS_WITHOUT_TRIGGERS, TRIGGERS_UNUSED,
  VARIABLES_UNUSED, DUPLICATE_NAMES
cross (4): UNLINKED_COUNTERS, GOALS_MISSING_IN_DIRECT,
  DIRECT_CLIENTS_AVAILABLE, CONTAINER_COUNTER_MISMATCH

НАДЁЖНОСТЬ
----------
- Изоляция аккаунтов: отозванный токен одного клиента не ломает прогон
  остальных (audit_all_accounts).
- Ошибки API переводятся в человеческий текст: Direct error_code 53, 54, 58,
  152, 8800; Метрика 401/403/404/429; YTM 401/403/404/429.
- Баллы: читаем заголовок Units, предупреждаем при остатке < 20 %.
- Самовосстановление полей: при ошибке 8000 («неизвестное поле») движок сам
  убирает лишние поля из запроса и повторяет. Защита от изменений в API.
- Раздел «Не проверено»: недоступная проверка видна в отчёте, а не исчезает
  молча.
- Токены никогда не выводятся: только маска вида y0__…f3a2.

КАК ПОДКЛЮЧИТЬ НОВЫЙ АККАУНТ
----------------------------
1) Список того, что уже выдано:
       discover_accounts()
2) Владелец выдаёт доступ. Вариант «одна кнопка»:
       oauth_link()  ->  отправить ссылку владельцу  ->  он присылает токен
3) Подключить:
       add_account(account='printyard', mode='token',
                   direct_token='...', metrica_token='...', ytm_token='...',
                   counter_ids=[106104483], container_ids=[1007795],
                   ulogin='anton.anima')
4) Проверить:
       check_account('printyard')
5) Первый аудит:
       audit_account('printyard')
Для агентского режима вместо токена: mode='agency', client_login='<логин>'
и переменная YANDEX_AGENCY_TOKEN в .env.

ОБРАТНАЯ СОВМЕСТИМОСТЬ
----------------------
Текущие три токена из .env становятся аккаунтом 'default'. Любой прежний
вызов без параметра account работает ровно как до правок. Файловый аудит
Коммандера не затронут. Требования не изменились: pandas, openpyxl, xlrd,
requests, python-dotenv, mcp.

ПРОВЕРЕНО НА РЕАЛЬНЫХ ДАННЫХ
----------------------------
tools count        = 29
checks registered  = 29
audit(default)     = 30 находок (4 ошибки / 15 предупреждений / 11 инфо)
check_account      = Директ ok (7 кампаний), Метрика ok (6 счётчиков), YTM ok
discover           = 4 владельца счётчиков; связка dmitrii.esikov -> chief_login
не проверено       = 0
упавших проверок   = 0

ДИАГНОСТИКА
-----------
Список инструментов:
  python -c "import sys; sys.path.insert(0,'C:/yandex_direct_agent');
              from mcp_instance import mcp; import tools;
              print(len(mcp._tool_manager._tools))"
Ожидаем: 29
Список проверок: list_audit_checks()
Состояние аккаунтов: list_accounts(), check_account('default')

ПРАВИЛА НА БУДУЩЕЕ (дополнение)
--------------------------------
- Новые инструменты работы с API пишутся ТОЛЬКО через слой access.py.
  Напрямую os.getenv("YANDEX_*_TOKEN") в tools/*.py больше не используем.
- Токены — только в secrets/secrets.json (или .env для аккаунта default).
  В accounts.json токенов быть не должно: он может попасть в git.
- Новый tools/*.py импортирует mcp из mcp_instance, как и раньше.
- Новая проверка аудита = функция с @register в audit_engine/checks_*.py.
  Массовые проблемы агрегируем по кампании, а не по одному объявлению.
- Каждая новая проверка должна корректно НЕ срабатывать там, где проблемы
  нет: это защищает от ложных находок, которые убивают доверие к отчёту.
- Дизайн-спека лежит в docs/superpowers/specs/
  2026-09-21-multi-account-audit-design.md

ЭТАП 6 — ПРАВКИ С DRY-RUN (тот же вечер, 21.09.2026)
--------------------------------------------------
Файлы: audit_engine/fixers.py (реестр правок), audit_engine/apply.py (план,
исполнение, журнал, откат). Инструментов стало 32 (было 29).

ЧТО ПРАВИТСЯ АВТОМАТИЧЕСКИ (2 фиксера)
-------------------------------------
DIRECT.TEXT_LENGTH    — сокращение заголовков до 56 и текстов до 81 символа.
                        Режем по границе слова, слова не рвём.
DIRECT.NO_DAILY_BUDGET — простановка дневного бюджета. Значение берётся из
                        реестра: account.direct.daily_budget (в рублях) или
                        строка "distributed". Если значение не задано —
                        фиксер ничего не делает и пишет пометку: придумывать
                        бюджет за клиента нельзя.

ЧТО НЕ ПРАВИТСЯ АВТОМАТИЧЕСКИ
-----------------------------
Всё остальное: структура групп и фраз, минус-фразы, модерация, цели Метрики,
Tag Manager. Инструмент apply_audit_fixes в ответе возвращает блок
not_automatic — сколько находок какого кода осталось на ручную работу.

ТРИ УРОВНЯ ЗАЩИТЫ
-----------------
1. apply_audit_fixes без confirm=True — режим плана: ни одного изменения в API,
   только чтение. В ответе warning: «Это план (dry-run), ничего не изменено».
2. Перед каждым изменением видна пара before/after: что было и что станет.
   Например: было «...с химчисткой мебели», станет «...с химчисткой».
3. После применения — журнал data/audits/apply_<аккаунт>_<время>.json
   с прежними значениями. Откат: rollback_apply(log_file='...', confirm=True).

ВАЖНОЕ НАБЛЮДЕНИЕ ПО СОКРАЩЕНИЮ ТЕКСТОВ
---------------------------------------
Автосокращение может убрать призыв к действию в конце текста. На реальном
примере: «...коммерческих помещений. Звоните» -> «...коммерческих помещений.»
Поэтому сначала смотрим план глазами, потом подтверждаем. Для этого dry-run
и сделан обязательным.

ЧТО ОТКАТИТЬ НЕЛЬЗЯ
-------------------
Снятие дневного бюджета через API невозможно: обратная операция — только
вручную в интерфейсе. rollback_apply честно перечисляет это в блоке
cannot_rollback, а не молчит.

ПРОВЕРЕНО (dry-run, без изменений аккаунта)
-------------------------------------------
tools  = 32, checks = 29, fixers = 2
apply_audit_fixes(confirm=False) -> actions_count = 4 (сокращение 4 текстов)
проверка неизменности аккаунта после dry-run: подтверждено (True)
откат по несуществующему журналу: корректная ошибка

ПРАВИЛА НА БУДУЩЕЕ (правки)
---------------------------
- Новый фиксер добавляется только с парой before/after и только для кода с
  fixable="auto". Иначе dry-run теряет смысл.
- Если для правки нужно решение человека (бюджет, смысл текста) — это НЕ
  фиксер, а рекомендация.
- Новая правка не должна требовать отката, который невозможно выполнить
  через API: такие случаи заранее помечаем в cannot_rollback.
- Автоматическое сокращение текста может убрать призыв к действию в конце.
  Поэтому план правок всегда просматривается человеком до подтверждения.

================================================================
ДОПОЛНЕНИЕ ОТ 22.09.2026 — ПОДКЛЮЧЕНИЕ АККАУНТА PRINTYARD
================================================================

СТАТУС АККАУНТА printyard (владелец anton.anima)
-----------------------------------------------
Метрика  — РАБОТАЕТ (ulogin=anton.anima)
           счётчики 106104483 (printyard.spb.ru новый) и 73028443 (Кладовкер)
YTM      — РАБОТАЕТ. Контейнер 1007795 привязан через token_ref='default':
           у нашего уже имеющегося YTM-токена есть доступ к этому контейнеру
           (он же связан со счётчиком 106104483), поэтому второй токен не нужен.
Директ   — НЕ РАБОТАЕТ. Ответ API:
           error_code 58, error_string «Незавершённая регистрация»,
           error_detail: «Необходимо заполнить для приложения заявку на доступ
           в интерфейсе Директа и дождаться её подтверждения».
           То есть блокировка на стороне приложения (OAuth-клиента), а не аккаунта.

ЧТО ЭТО ЗНАЧИТ И ЧТО ДЕЛАТЬ
--------------------------
Ошибка 58 в таком виде — не про логин и не про права представителя. Новое
OAuth-приложение должно пройти регистрацию в Директе: подать заявку на доступ
to API и дождаться подтверждения.
Вариант A (ждём): подтвердить заявку для нового приложения — токен заработает сам.
Вариант B (быстрее, если нужен Директ сегодня): у нас уже есть приложение,
  которое в Директе работает — именно его токен читает кампании аккаунта default.
  Достаточно взять его ClientID, положить в .env как YANDEX_OAUTH_CLIENT_ID
  и попросить anton.anima пройти по ссылке из oauth_link(). Токен будет выдан
  под его логином и контур Директа заработает сразу, без ожидания заявки.

АУДИТ PRINTYARD (что получилось при живом прогоне)
--------------------------------------------------
findings=8 (ошибок 0, предупреждений 3, инфо 5)
не проверено: campaigns.get и reports/campaigns — обе из-за ошибки 58
Диагнозы: «Качество данных Метрики под вопросом» (29),
         «Разметка Tag Manager не срабатывает» (15)
Вывод: без Директа аудит по аккаунту неполный — проверки структуры,
текстов, бюджетов и расхода остаются в разделе «Не проверено».

ИСПРАВЛЕНИЯ ОТ 22.09.2026
-------------------------
1. tools/reports.py — добавлена _decode(): Яндекс отдаёт JSON и TSV в UTF-8,
   но не всегда указывает charset, из-за чего в текстах ошибок и в названиях
   кампаний получалась кракозябра. Теперь декодируем явно.
2. access.py — у metrica_get и ytm_get добавлена повторная попытка при
   таймауте (и увеличенный таймаут на второй попытке). Причина: за один день
   дважды ловили read timeout от api.ytm.yandex.net и api-metrika.yandex.net
   на ровном месте. Без retry проверка уезжала в «Не проверено» напрасно.
Проверено: audit(default) снова даёт 30 находок и 0 непроверенного,
           audit(printyard) — 8 находок и 2 непроверенного (только Директ).

ЕЩЁ ОДНО ПРАВИЛО НА БУДУЩЕЕ
---------------------------
Перед подключением нового аккаунта проверять, не достаточно ли уже имеющихся
токенов: если счётчики и контейнеры уже видны — токены писать не надо, а YTM и
Метрику привязывать через token_ref на общий токен.

ПОДКЛЮЧЕНИЕ PRINTYARD ЗАВЕРШЕНО (три из трёх контуров)
-----------------------------------------------------
Путь решения: владелец авторизовал НАШЕ одобренное приложение
"Yandex Direct Agent" (ClientID ba7bb4d996c34d72b95a653be53da88a)
под своим логином. Заявка не потребовалась — справка подтвердила, что
после одобрения с приложением может работать неограниченное число
пользователей. Это работает: полученный токен сразу дал 27 кампаний.
ClientID прописан в .env как YANDEX_OAUTH_CLIENT_ID.
Токи по контурам: Директ — токен от нашего приложения (под его логином),
Метрика — его токен, YTM — наш общий токен через token_ref='default'.

РЕЕСТР PRINTYARD (дополнено)
----------------------------
campaign_ids: 710718813, 710909401, 114807904, 117845305, 706473673 —
  teper' resolve('710718813') sam nahodit akkaunt.
goals.target_cpa: 550 — bez etogo ne rabotala proverka CPA_ABOVE_TARGET.
Kontejner 1007795 ubran iz akkaunta 'default': on privyazan k schetchiku
  106104483, to est' prinadlezhit printyard. Inache resolve('1007795')
  vozvrashchal ne tot akkaunt.

АУДИТ PRINTYARD НА ЖИВЫХ КАМПАНИЯХ
----------------------------------
findings=237 (ошибок 18, предупреждений 48, инфо 171), не проверено 0.
Топ диагнозов по impact:
  100 — Расход есть, конверсий нет (кампания 714182406: 151 клик, 2 471 ₽, 0 конверсий)
   98 — Тексты не проходят по длине (222 объявления в 17 кампаниях)
   62 — Цена конверсии выше целевой (710718813: CPA 3 593 ₽ при целевой 550 ₽)
   35 — Дневной бюджет не задан (6 кампаний)
   29 — Качество данных Метрики под вопросом
   15 — Разметка Tag Manager не срабатывает
Замечание: 166 находок из 237 — это DIRECT.GROUP_NO_NEGATIVES с impact 5.
На общую картину не влияет (диагнозы строятся от impact 25+), но ленту
находок шумит. При желании свернём их в одну строку на кампанию.

НАЙДЕННЫЕ И ИСПРАВЛЕННЫЕ ДЕФЕКТЫ (22.09.2026, вторая волна)
--------------------------------------------------------
1. ID_CHUNK 500 -> 10 в fetchers.py. Direct API принимает не более
   10 идентификаторов в SelectionCriteria (Ids / CampaignIds / AdGroupIds)
   за один запрос. Проверено экспериментально: 10 — ок, 15 — ошибка 4001.
   С 27 кампаниями падали сразу три проверки: группы, объявления, фразы.
2. Главное: защита от ЛОЖНЫХ находок. Раньше неудачная загрузка была
   неотличима от пустого результата, и на упавшем запросе проверки
   объявляли "кампания не наполнена" для ВСЕХ кампаний сразу.
   Теперь при ошибке в data пишется None, а у контекста появился метод
   ctx.loaded(*ключи): проверка либо пропускается, либо работает только
   на реально загруженных данных.
3. Проверка CPA читала целевое значение из ctx.data, а оно лежит в
   ctx.target_cpa — из-за этого CPA_ABOVE_TARGET не срабатывал вообще.
4. Формулировка в диагнозе по длине текстов приведена в читаемый вид.

СОСТОЯНИЕ ЗАЯВОК НА ДОСТУП К API (на 22.09.2026)
------------------------------------------------|
Yandex Direct Agent        ba7bb4d996c34d72b95a653be53da88a
  заявка от 17.09.2026, доступ полный, статус ОДОБРЕНА
  Это единственное одобренное приложение, на нём и работает Директ.

Yandex Direct Agent Metrica 3f7692ca2f18447aaf983b5514a96f1f
  заявка от 22.09.2026, доступ полный, статус НОВАЯ
  Подана ради: снять зависимость от единственного приложения,
  объединить Директ и Метрику в одном токене вместо двух.

Сроки рассмотрения: рабочие дни РФ 10:00–19:00, от часа до трёх рабочих
суток, в пик — до семи.

ПОСЛЕ ОДОБРЕНИЯ ЗАЯВКИ — ЧТО ДЕЛАТЬ
-----------------------------------
Шаг 1. Проверить результат одной командой:
       python apps_check.py
   Признак успеха: у строки YANDEX_METRICA_TOKEN в колонке ДИРЕКТ было
   [58] Незавершенная регистрация — должно стать OK.

Шаг 2. Если Директ стал OK — объединить контуры на одном токене:
   - для аккаунта default: set_account_tokens('default', direct_token=<
     токен приложения 3f7692ca>), затем check_account('default');
   - для printyard: попросить владельца один раз пройти по ссылке
     oauth_link() (ClientID объединённого приложения), затем
     set_account_tokens('printyard', direct_token=<новый токен>,
     metrica_token=<он же>). Привязки счётчиков и кампаний не теряются.

Шаг 3. Проверить: check_account('printyard') должен дать три OK,
       audit_account('printyard') — ноль непроверенного.

Важно: пока заявка НОВАЯ, её можно исправить кнопкой «Редактировать».
Следите, чтобы в «Основных функциях приложения» НЕ были отмечены
«Управление ставками (биддинг)» и «Подбор ключевых слов (wordstat)» —
мы этого не делаем.
